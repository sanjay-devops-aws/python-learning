# pip list
# pip help
# pip help install
# pip search Pympler
# pip list -o
# pip freeze --local | grep -v '^\-e' | cut -d = -f 1 | xargs -n1 pip install -U  # update all package at time
# pip list -U setuptool
# pip freeze
# pip install -r  test.txt
# pip install --outdated
